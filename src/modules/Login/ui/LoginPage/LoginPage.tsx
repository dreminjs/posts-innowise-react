import { LoginForm } from "../LoginForm/LoginForm";
import { LoginLayout } from "./LoginLayout";

export const LoginPage = () => {
  return (
    <LoginLayout>
      <LoginForm />
    </LoginLayout>
  );
};
